import React from 'react';
import { View, Text, Image, TouchableOpacity } from 'react-native';

const CallApiScreen = ({ navigation }) => {
  return (
    <View>
      <Text>CallApiScreen</Text>
    </View>
  );
};
export default CallApiScreen;
