import React from 'react';
import { View, Text, Image, TouchableOpacity } from 'react-native';

const ContactApiScreen = ({ navigation }) => {
  return (
    <View>
      <Text>ContactApiScreen</Text>
    </View>
  );
};
export default ContactApiScreen;
