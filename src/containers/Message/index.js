import React from 'react';
import { View, Text, Image, TouchableOpacity } from 'react-native';

const MessageApiScreen = ({ navigation }) => {
  return (
    <View>
      <Text>MessageApiScreen</Text>
    </View>
  );
};
export default MessageApiScreen;
